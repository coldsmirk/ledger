// Conventional Commits, enforced single-line (no body, no footer) — shared config from
// github.com/coldsmirk/canon.
import { defineCommitlintConfig } from "@coldsmirk/commitlint-config";

export default defineCommitlintConfig();
