// Plain-CSS rules for the package stylesheets — shared config from github.com/coldsmirk/canon.
// Enforces the kebab-case class contract mechanically (docs/DESIGN.md § Styling contract).
import { defineStylelintConfig } from "@coldsmirk/stylelint-config";

export default defineStylelintConfig();
